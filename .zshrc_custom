if [[ "$USER" == "danielblack" ]]
then
    # This is for your personal Macbook Pro.
    export PATH="/Users/danielblack/.rbenv/shims:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin:/opt/X11/bin:/usr/local/MacGPG2/bin"
    export PATH="/Users/danielblack/miniconda3/bin:$PATH"
    alias sshpi='ssh pi@192.168.50.1'
elif [[ "$USER" == "blackds2" ]]
then
    # This is for your personal Macbook Pro.
    export PATH="/usr/local/bin:/sur/bin:/bin:/usr/sbin:/Users/blackds2/Library/Python/2.7/bin"
fi

alias hgrep='history | grep $1'
alias jupyter='export BROWSER=firefox && jupyter && export BROWSER=links'
alias jupyter-lab='export BROWSER=firefox && jupyter && export BROWSER=links'
alias clear='clear&&task'
alias toast='powershell.exe -command New-BurntToastNotification'
#alias ddgr='ddgr --url-handler links'
function g { links http://www.google.com/search\?q="$1" ; }

# Enables completion for um.
#if [ -f $(brew --prefix)/etc/bash_completion.d/um-completion.sh ]; then
#  . $(brew --prefix)/etc/bash_completion.d/um-completion.sh
#fi

export VISUAL=vim
export BROWSER=links
# export BROWSER=w3m
export EDITOR="$VISUAL"
export PATH="/usr/local/opt/vim/bin/vim:/usr/local/sbin:$PATH"



#THIS WAS ACTUALLY COPIED AND HACKED FROM .bashrc 190604 @ 0356
# >>> conda initialize >>>
# !! Contents within this block are managed by 'conda init' !!
__conda_setup="$('/home/dblack/anaconda3/bin/conda' 'shell.zsh' 'hook' 2> /dev/null)"
if [ $? -eq 0 ]; then
    eval "$__conda_setup"
else
    if [ -f "/home/dblack/anaconda3/etc/profile.d/conda.sh" ]; then
        . "/home/dblack/anaconda3/etc/profile.d/conda.sh"
    else
        export PATH="/home/dblack/anaconda3/bin:$PATH"
    fi
fi
unset __conda_setup
# <<< conda initialize <<<
# Screw it. Shit ain't workin'.

if [[ "$USER" == "muraii" ]]
then
    export PATH="/home/muraii/anaconda3/bin:$PATH"
elif [[ "$USER" == "dblack" ]]
then
    export PATH="/home/dblack/anaconda3/bin:$PATH"
fi

# Fix the issue with timezones on WSL going kaput or something gosh.
export TZ='US/Eastern'

# Getting X running from WSL

if [[ "$USER" == "dblack" ]]
then
    DISPLAY=0:0
    export DISPLAY
fi
